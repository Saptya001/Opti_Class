# Server configuration
PORT=5000
NODE_ENV=development

# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=yourpassword
DB_NAME=opticalsscheduler

# JWT secret (use a strong random string in production)
JWT_SECRET=supersecretjwtkey

# Logging
LOG_LEVEL=info
